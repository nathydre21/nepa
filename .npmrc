# Use public npm registry only (no login required for contributors)
registry=https://registry.npmjs.org/

# Ignore peer dependency conflicts (helps install succeed for contributors)
legacy-peer-deps=true

# Optional: reduce install noise
save-exact=false
engine-strict=false
